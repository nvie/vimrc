Installation
============

Symlink ~/.config/nvim to this project.

```bash
$ mkdir -p ~.config
$ cd ~/.config
$ ln -s ~/Projects/vimrc/nvim
$ nvim ~/.config/nvim/init.vim
```

And run: `:PlugInstall`.
